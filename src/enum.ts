export enum MessageType { Info = 1, Success, Warning, Error }

export enum Status { Inactive = 0, Active = 1 }

export enum KeyCode {
    Backspace = 8,
    Enter = 13,
    Escape = 27,
    ArrowLeft = 37,
    ArrowUp = 38,
    ArrowRight = 39,
    ArrowDown = 40,
}
